## ENCONTRE A SOLUÇÃOO PARA O PROBLEMA! **

## Pergunte o nome do 'USUÁRIO' e diga Olá: "NOME DO USUÁRIO" .

//// ex: [
    let nome = prompt()
    alert("Olá: " + nome)
]

## MELHORAR MINHA LÓGICA DE PROGRAMAÇÃO

- perguntas corretas


## Métodos:
- toFixed()    -- as casas decimas

## Gerar números aleatórios
 - math.randon() -> Número aleatório
 - math.ceil() -> aredonar para cima  1.2
 - math.floor(1.2)  -> aredonadmento para o piso  1.1

    ex: 
    Math.ceil(Math.random() * 10)  -> aredonda pra cima;

   Math.round(Math.random() * 10)  -> aredonda tanto pra cima quanto pra baixo;

   ## CALCULLO DO IMC


    1- CALCULO DE IMC:

         °1 ex:  

        feito assim    [  peso / ( altura * altura)  ]   ou [ peso / (altura ** 2)  ] =>peso / por altura (** 2) <-elevado ao quadrado

        °2 ex: completo do calculo de IMC

        feito assim:: 
          (  peso / ( ( altura / 100) ** 2 )  ).toFixed(2)


        
